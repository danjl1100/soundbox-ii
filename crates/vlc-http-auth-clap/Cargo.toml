[package]
name = "vlc-http-auth-clap"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.4", features = ["derive", "env"] }
vlc-http-auth = { version = "0.1.0", path = "../vlc-http-auth" }

[lints]
workspace = true
